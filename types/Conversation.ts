export type Conversation = {
  conversation_id: string;
  conversation_name: string;
  conversation_url: string;
  callback_url: string;
  status: string;
  replica_id: string;
  persona_id: string;
  created_at: string;
  updated_at: string;
};
