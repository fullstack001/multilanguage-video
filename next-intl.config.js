module.exports = {
    locales: ['en', 'fa'], // List all the locales your project supports
    defaultLocale: 'en',   // Set the default locale
    // Add the following line to ensure the config is recognized
    // This is optional based on your project needs
    // i18n: { locales: ['en', 'fa'], defaultLocale: 'en' }
};
